import styled from "styled-components";

const Alert = () => {

  return (
    <Container>
      Alert
    </Container>
  )
}

export default Alert;

const Container = styled.div`
  position: absolute;
  top: 50%;
  z-index: 100;
`